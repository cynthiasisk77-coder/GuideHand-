// Max. The name is hers, and it is spoken as well as printed, so it lives
// here on its own: readAloud and the voice screens need it without dragging
// the whole article library in behind it.
export const AI_NAME = 'Max';
